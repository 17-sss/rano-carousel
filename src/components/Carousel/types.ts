type TCarousel = {
  infiniteLoop?: boolean;
  thumbWidth?: number;
  autoPlay?: boolean;
  interval?: number;
  stopOnHover?: boolean;
  showArrows?: boolean;
  swipeable?: boolean;
};

export type { TCarousel };
